/* This command doesn't work on early versions of sqlite3
   earlier than 3.8.10. Use '.schema' instead
   It displays the schema (which are the create commands)
   for the tables in the database. */

.fullschema
